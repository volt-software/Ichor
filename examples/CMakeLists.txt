if(USE_RAPIDJSON)
    file(GLOB_RECURSE PROJECT_SERIALIZER_EXAMPLE_SOURCES ${TOP_DIR}/examples/serializer_example/*.cpp)
    add_executable(cppelix_serializer_example ${PROJECT_SERIALIZER_EXAMPLE_SOURCES})
    if(USE_SPDLOG)
        target_sources(cppelix_serializer_example PRIVATE ${SPDLOG_SOURCES})
    endif()
    target_link_libraries(cppelix_serializer_example ${CMAKE_THREAD_LIBS_INIT})
    target_link_libraries(cppelix_serializer_example cppelix)
endif()

file(GLOB_RECURSE PROJECT_TRACKER_EXAMPLE_SOURCES ${TOP_DIR}/examples/tracker_example/*.cpp)
add_executable(cppelix_tracker_example ${PROJECT_TRACKER_EXAMPLE_SOURCES})
if(USE_SPDLOG)
    target_sources(cppelix_tracker_example PRIVATE ${SPDLOG_SOURCES})
endif()
target_link_libraries(cppelix_tracker_example ${CMAKE_THREAD_LIBS_INIT})
target_link_libraries(cppelix_tracker_example cppelix)

file(GLOB_RECURSE PROJECT_TIMER_EXAMPLE_SOURCES ${TOP_DIR}/examples/timer_example/*.cpp)
add_executable(cppelix_timer_example ${PROJECT_TIMER_EXAMPLE_SOURCES})
if(USE_SPDLOG)
    target_sources(cppelix_timer_example PRIVATE ${SPDLOG_SOURCES})
endif()
target_link_libraries(cppelix_timer_example ${CMAKE_THREAD_LIBS_INIT})
target_link_libraries(cppelix_timer_example cppelix)

file(GLOB_RECURSE PROJECT_MULTITHREADED_EXAMPLE_SOURCES ${TOP_DIR}/examples/multithreaded_example/*.cpp)
add_executable(cppelix_multithreaded_example ${PROJECT_MULTITHREADED_EXAMPLE_SOURCES})
if(USE_SPDLOG)
    target_sources(cppelix_multithreaded_example PRIVATE ${SPDLOG_SOURCES})
endif()
target_link_libraries(cppelix_multithreaded_example ${CMAKE_THREAD_LIBS_INIT})
target_link_libraries(cppelix_multithreaded_example cppelix)

if(BUILD_CELIX_SHIM)
    file(GLOB_RECURSE PROJECT_CELIX_EXAMPLE_SOURCES ${TOP_DIR}/examples/celix_example/*.cpp)
    add_executable(cppelix_celix_example ${PROJECT_CELIX_EXAMPLE_SOURCES})
    if(USE_SPDLOG)
        target_sources(cppelix_celix_example PRIVATE ${SPDLOG_SOURCES})
    endif()
    target_link_libraries(cppelix_celix_example ${CMAKE_THREAD_LIBS_INIT})
    target_link_libraries(cppelix_celix_example cppelix)
    target_link_libraries(cppelix_celix_example cppelix_celix_shim)
endif()