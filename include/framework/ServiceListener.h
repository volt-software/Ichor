#pragma once

#include <unordered_map>
#include <vector>

namespace Cppelix {
    enum class ServiceEvent {
        MODIFIED,
        MODIFIED_ENDMATCH,
        REGISTERED,
        UNREGISTERING
    };

    class ServiceListener {
    public:

    private:
    };
}