if(USE_RAPIDJSON)
    file(GLOB_RECURSE PROJECT_EXAMPLE_SOURCES ${TOP_DIR}/benchmarks/serializer_benchmark/*.cpp)
    add_executable(cppelix_serializer_benchmark ${PROJECT_EXAMPLE_SOURCES})
    target_link_libraries(cppelix_serializer_benchmark ${CMAKE_THREAD_LIBS_INIT})
    target_link_libraries(cppelix_serializer_benchmark cppelix)
endif()

file(GLOB_RECURSE PROJECT_EXAMPLE_SOURCES ${TOP_DIR}/benchmarks/start_stop_benchmark/*.cpp)
add_executable(cppelix_start_stop_benchmark ${PROJECT_EXAMPLE_SOURCES})
target_link_libraries(cppelix_start_stop_benchmark ${CMAKE_THREAD_LIBS_INIT})
target_link_libraries(cppelix_start_stop_benchmark cppelix)